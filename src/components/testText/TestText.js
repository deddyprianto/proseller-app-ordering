import React, {useEffect} from 'react';
import {StyleSheet, View, TouchableOpacity} from 'react-native';

const TestText = ({children}) => {
  return <View>{children}</View>;
};

export default TestText;
